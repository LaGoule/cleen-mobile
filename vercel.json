{
  "version": 2,
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ],
  "builds": [
    { "src": "angular.json", "use": "@vercel/static-build", "config": { "distDir": "dist/cleen-mobile" } }
  ]
}